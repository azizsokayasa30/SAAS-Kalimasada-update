# FreeRADIUS SQL module — SQLite (billing-kalimasada)
# Deploy: diganti path DB oleh scripts/install-freeradius-billing.sh
#   sudo cp deploy/freeradius-mods-sql-sqlite.conf /etc/freeradius/3.0/mods-enabled/sql
#
# WAJIB: jangan simpan file *.bak* di mods-enabled/ (FreeRADIUS memuat semua file di folder itu).

sql {
	dialect = "sqlite"
	driver = "rlm_sql_${dialect}"

	sqlite {
		filename = "__RADIUS_DB_PATH__"
		busy_timeout = 60000
	}

	max_connections = 1

	acct_table1 = "radacct"
	acct_table2 = "radacct"
	postauth_table = "radpostauth"
	authcheck_table = "radcheck"
	groupcheck_table = "radgroupcheck"
	authreply_table = "radreply"
	groupreply_table = "radgroupreply"
	usergroup_table = "radusergroup"

	delete_stale_sessions = yes

	read_clients = yes
	client_table = "nas"
	group_attribute = "SQL-Group"

	$INCLUDE ${modconfdir}/${.:name}/main/${dialect}/queries.conf
}
